#!/usr/bin/env ruby
$stdout.sync = true
root = File.expand_path('..', __dir__)
Dir.chdir(root)
%w[http_request static web_socket client game server].each { |file| require "#{root}/server/#{file}" }

Server.new(Integer(ARGV[0] || 8765), Game.new, root).run
